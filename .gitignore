# data & caches (datasets are downloaded on first run)
data_cache/
.venv/
__pycache__/
*.pyc
# large intermediate artifacts (regenerated by the scripts)
run_*.log
# blog-only byproducts
article*.html
medium_*.txt
node_modules/
.DS_Store
# latex build
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.synctex.gz

# unrelated large sub-project (separate benchmark study) — keep out of this repo
benchmark/
*.pkl
